[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python app.py"
# healthcheck отключён — при первом запуске может мешать
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
